language: julia

os:
  - linux
  - osx

julia:
  - 1   # current stable
  - 1.1 # minimum supported version
  - nightly

branches:
  only:
  - master

notifications:
  email: false

script:
  - julia --project --color=yes -e 'using Pkg;
                                    Pkg.instantiate();
                                    Pkg.test();'
